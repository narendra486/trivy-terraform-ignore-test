terraform {
  required_version = ">= 1.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = "us-east-1"
}

# Intentionally insecure resources for Trivy CI testing.
# These findings are expected; some are suppressed via .trivyignore.yaml.
